import React, { useState, useEffect } from 'react';
import Book from '../../models/Book';

interface PropTypes {
    book: Book | null;
}

const BookView = (props: PropTypes) : JSX.Element => {

    return (
        <div>

        </div>
    );
};

export default BookView;